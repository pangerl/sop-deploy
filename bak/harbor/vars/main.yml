
HARBOR_VER: "v2.5.3"

HARBOR_VER_MAIN: "{{ HARBOR_VER.split('.')[0] }}.{{ HARBOR_VER.split('.')[1] }}"

# HARBOR_HOSTNAME 值设置
HARBOR_HOSTNAME: "{{ inventory_hostname }}"

# harobr 默认安装选项
HARBOR_INST_OPS: ""
